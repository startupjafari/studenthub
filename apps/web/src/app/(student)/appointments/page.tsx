import { StudentAppointmentsView } from '../../../views/appointments'

// «Запись в деканат» студента (задача 16). Оболочка — (student)/layout.tsx.
export default function Page() {
  return <StudentAppointmentsView />
}
