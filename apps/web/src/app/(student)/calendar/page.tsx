import { CalendarView } from '../../../views/calendar'

// Единый академический календарь студента (задача 9). Оболочка — (student)/layout.tsx.
export default function Page() {
  return <CalendarView />
}
