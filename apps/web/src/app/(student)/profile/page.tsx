import { UserProfile } from '../../../widgets/user-profile'

// Профиль внутри оболочки (student): сайдбар/топбар сохраняются на странице.
export default function ProfilePage() {
  return <UserProfile />
}
