import { StudentToday } from '../../../widgets/today'

// Главный ежедневный экран студента (задача 1). Оболочка — в (student)/layout.tsx.
export default function Page() {
  return <StudentToday />
}
