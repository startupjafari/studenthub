'use client'

// error.tsx обязателен для каждого сегмента (§2.2). Всегда клиентский компонент.
// Разметка и отправка ошибки в Sentry — в общем ErrorScreen (Ф13.8).
export { ErrorScreen as default } from '../../../shared/ui'
