import { TeacherToday } from '../../widgets/today'

// Дашборд преподавателя — рабочий экран дня: следующая пара, расписание на сегодня,
// что требует внимания и последние изменения. Раньше здесь была сетка плиток-ссылок
// по разделам роли, то есть дословный повтор сайдбара слева, а рядом в навигации
// стоял отдельный пункт «Сегодня» с реальным содержимым. Разделы объединены: плитки
// убраны, «Сегодня» переехало сюда.
export default function Page() {
  return <TeacherToday />
}
