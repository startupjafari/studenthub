import { TeacherToday } from '../../../views/today'

// Главный ежедневный экран преподавателя (задача 1). Оболочка — в teacher/layout.tsx.
export default function Page() {
  return <TeacherToday />
}
