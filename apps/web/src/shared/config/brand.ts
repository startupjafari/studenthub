// Брендовая заливка: диагональный градиент от primary в фиолетовый.
//
// Используется там, где нужна фирменная подложка вместо картинки: обложка профиля,
// плитка текстового поста в ленте, тот же пост в лайтбоксе. Раньше строка была
// скопирована в четырёх местах — разъезжалась при любой правке и тянула за собой
// сырые цвета Tailwind в код экранов (docs/DESIGN_SYSTEM.md §2.2).
//
// Средняя и конечная точки — сырые оттенки осознанно: это декоративный бренд-градиент,
// а не статус, и токена такой роли в системе нет. Место у него ровно одно — здесь.
export const BRAND_GRADIENT = 'bg-gradient-to-br from-primary via-indigo-500 to-violet-500'
