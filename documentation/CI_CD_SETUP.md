# Быстрая настройка CI/CD

## ✅ Что было создано

### 1. GitHub Actions Workflows

#### `.github/workflows/ci.yml`
Основной CI workflow с:
- ✅ Проверка линтинга и форматирования
- ✅ Сборка проекта
- ✅ Запуск тестов (когда настроен Jest)
- ✅ Проверка безопасности
- ✅ Проверка типов TypeScript

#### `.github/workflows/deploy.yml`
Workflow для развертывания:
- ✅ Автоматический деплой в staging при push в `main`
- ✅ Автоматический деплой в production при создании тега `v*`
- ⚠️ Требует настройки команд деплоя

#### `.github/workflows/pr-check.yml`
Проверка Pull Request:
- ✅ Проверка на `console.log`
- ✅ Проверка TODO/FIXME
- ✅ Проверка commit сообщений

#### `.github/workflows/dependabot-auto-merge.yml`
Автоматическое слияние Dependabot PR:
- ✅ Автоматическая проверка и слияние обновлений зависимостей

### 2. Dependabot конфигурация

#### `.github/dependabot.yml`
Автоматическое обновление:
- ✅ NPM зависимости (еженедельно)
- ✅ GitHub Actions (еженедельно)
- ✅ Docker образы (еженедельно)

### 3. Шаблоны Issues и PR

#### `.github/PULL_REQUEST_TEMPLATE.md`
Шаблон для Pull Request с чек-листом

#### `.github/ISSUE_TEMPLATE/bug_report.md`
Шаблон для сообщений об ошибках

#### `.github/ISSUE_TEMPLATE/feature_request.md`
Шаблон для запросов функций

---

## 🚀 Быстрый старт

### 1. Проверьте, что все файлы на месте

```bash
ls -la .github/workflows/
# Должны быть: ci.yml, deploy.yml, pr-check.yml, dependabot-auto-merge.yml

ls -la .github/
# Должен быть: dependabot.yml
```

### 2. Настройте GitHub Secrets (если нужно)

1. Перейдите в Settings → Secrets and variables → Actions
2. Добавьте секреты:
   - `DATABASE_URL` (для тестов, опционально)
   - `CODECOV_TOKEN` (для покрытия, опционально)

### 3. Настройте Environments

1. Settings → Environments
2. Создайте `staging` и `production`
3. Добавьте секреты для деплоя (если нужно)

### 4. Настройте команды деплоя

Откройте `.github/workflows/deploy.yml` и замените комментарии на реальные команды деплоя.

### 5. Протестируйте

```bash
# Создайте тестовый PR
git checkout -b test-ci
git commit --allow-empty -m "test: CI workflow"
git push origin test-ci
```

Создайте Pull Request и проверьте, что CI запустился.

---

## 📋 Что нужно настроить

### Обязательно

- [ ] Команды деплоя в `deploy.yml` (строки с комментариями)

### Опционально

- [ ] GitHub Secrets для тестов
- [ ] Environments для деплоя
- [ ] Codecov токен
- [ ] Commitlint (для проверки commit сообщений)

---

## 🎯 Как использовать

### CI запускается автоматически при:
- Push в `main` или `develop`
- Создании Pull Request

### Деплой

**Staging:**
```bash
git push origin main
```

**Production:**
```bash
git tag v1.0.0
git push origin v1.0.0
```

---

## 📊 Мониторинг

- **Actions вкладка** в GitHub - все запуски workflow
- **Pull Request** - статус проверок
- **Environments** - статус деплоя

---

## 🔗 Дополнительная документация

- [CI_CD.md](CI_CD.md) - Полная документация по CI/CD
- [GitHub Actions Docs](https://docs.github.com/en/actions)

---

**Готово!** CI/CD настроен и готов к использованию! 🚀
