# Локальный запуск проекта StudentHub

## 📋 Что нужно для запуска проекта локально?

### Обязательные требования

1. **Node.js** версии 18 или выше
   - Скачать: https://nodejs.org/
   - Проверить версию: `node --version`
   - Рекомендуется использовать LTS версию

2. **npm** или **yarn** (устанавливается вместе с Node.js)
   - Проверить версию: `npm --version`

3. **PostgreSQL** версии 14 или выше
   - Скачать: https://www.postgresql.org/download/
   - Или использовать Docker (рекомендуется)

4. **Redis** версии 7 или выше
   - Скачать: https://redis.io/download
   - Или использовать Docker (рекомендуется)

5. **Docker Desktop** (рекомендуется)
   - Скачать: https://www.docker.com/products/docker-desktop
   - Упрощает запуск PostgreSQL и Redis

6. **Git**
   - Скачать: https://git-scm.com/downloads

### Опциональные инструменты

- **Postman** - для тестирования API
- **VS Code** или другой редактор кода
- **DBeaver** или **pgAdmin** - для работы с базой данных

---

## 🚀 Пошаговая инструкция по запуску

### Шаг 1: Клонирование репозитория

```bash
git clone <repository-url>
cd studenthub
```

### Шаг 2: Установка зависимостей Backend

```bash
cd studenthub-backend
npm install
```

**Ожидаемое время:** 2-5 минут

### Шаг 3: Установка зависимостей Frontend

```bash
cd ../studenthub-frontend
npm install
```

**Ожидаемое время:** 3-7 минут

### Шаг 4: Настройка переменных окружения

#### Backend (.env файл)

Создайте файл `.env` в папке `studenthub-backend/`:

```env
# ============================================
# Окружение
# ============================================
NODE_ENV=development
PORT=3000

# ============================================
# База данных
# ============================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=studenthub
POSTGRES_PORT=5432

# Прямое подключение к PostgreSQL (для разработки)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/studenthub?schema=public

# ============================================
# Redis
# ============================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ============================================
# JWT
# ============================================
JWT_SECRET=dev-secret-key-change-in-production-min-32-chars
JWT_EXPIRATION=15m
JWT_REFRESH_SECRET=dev-refresh-secret-key-change-in-production-min-32-chars
JWT_REFRESH_EXPIRATION=7d

# ============================================
# Email (SMTP)
# ============================================
# Для разработки можно использовать Mailtrap или аналогичный сервис
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_SECURE=false
SMTP_USER=your-mailtrap-user
SMTP_PASSWORD=your-mailtrap-password
SMTP_FROM_EMAIL=noreply@studenthub.local

# ============================================
# Frontend URL
# ============================================
FRONTEND_URL=http://localhost:3000

# ============================================
# App
# ============================================
APP_NAME=StudentHub
```

#### Frontend (.env.local файл)

Создайте файл `.env.local` в папке `studenthub-frontend/`:

```env
# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000/api
NEXT_PUBLIC_WS_URL=http://localhost:3000

# App Configuration
NEXT_PUBLIC_APP_NAME=StudentHub
```

### Шаг 5: Запуск базы данных и Redis через Docker

```bash
# Перейти в папку backend
cd studenthub-backend

# Запустить PostgreSQL и Redis
docker compose up -d

# Проверить статус
docker compose ps
```

**Ожидаемое время:** 1-2 минуты

**Проверка:**
- PostgreSQL должен быть доступен на `localhost:5432`
- Redis должен быть доступен на `localhost:6379`

### Шаг 6: Применение миграций базы данных

```bash
# В папке studenthub-backend
npm run db:migrate:prod

# Генерация Prisma Client
npm run db:generate
```

**Что происходит:**
- Создаются все таблицы в базе данных
- Применяются все миграции
- Генерируется Prisma Client для работы с БД

### Шаг 7: Запуск Backend сервера

```bash
# В папке studenthub-backend
npm run start:dev
```

**Ожидаемый результат:**
```
[Nest] INFO  [NestFactory] Starting Nest application...
[Nest] INFO  [InstanceLoader] AppModule dependencies initialized
[Nest] INFO  [NestFactory] Nest application successfully started
[Nest] INFO  Application is running on: http://localhost:3000
```

**Проверка:**
- Откройте в браузере: http://localhost:3000/api/health
- Должен вернуться статус `200 OK`

### Шаг 8: Запуск Frontend приложения

Откройте **новый терминал**:

```bash
# Перейти в папку frontend
cd studenthub-frontend

# Запустить dev сервер
npm run dev
```

**Ожидаемый результат:**
```
▲ Next.js 16.1.2
- Local:        http://localhost:3000
- ready started server on 0.0.0.0:3000
```

**Проверка:**
- Откройте в браузере: http://localhost:3000
- Должна открыться страница приложения

---

## ✅ Проверка работоспособности

### 1. Проверка Backend API

```bash
# Health check
curl http://localhost:3000/api/health

# Swagger документация
# Откройте в браузере: http://localhost:3000/api/docs
```

### 2. Проверка Frontend

- Откройте http://localhost:3000
- Должна открыться страница входа или главная страница

### 3. Проверка базы данных

```bash
# Подключиться к PostgreSQL через Docker
docker exec -it studenthub_postgres psql -U postgres -d studenthub

# Проверить таблицы
\dt

# Выйти
\q
```

### 4. Проверка Redis

```bash
# Подключиться к Redis через Docker
docker exec -it studenthub_redis redis-cli

# Проверить работу
PING
# Должен вернуться: PONG

# Выйти
exit
```

---

## 🔧 Альтернативные способы запуска

### Вариант 1: Без Docker (локальная установка)

Если у вас установлены PostgreSQL и Redis локально:

1. Установите PostgreSQL и создайте базу данных:
```sql
CREATE DATABASE studenthub;
```

2. Установите Redis и запустите сервис

3. Обновите `.env` файл с правильными хостами

4. Выполните миграции и запустите серверы

### Вариант 2: Только Backend (без Frontend)

Если нужно только протестировать API:

```bash
cd studenthub-backend
docker compose up -d
npm run db:migrate:prod
npm run db:generate
npm run start:dev
```

API будет доступен на http://localhost:3000/api

### Вариант 3: Production-подобная конфигурация

Для тестирования масштабирования используйте:

```bash
cd studenthub-backend
docker compose -f docker-compose.loadbalancer.yml up -d
```

Подробнее: [DOCKER_SETUP.md](DOCKER_SETUP.md)

---

## 🐛 Решение проблем

### Проблема: PostgreSQL не запускается

**Решение:**
```bash
# Проверить логи
docker compose logs postgres

# Пересоздать контейнер
docker compose down -v
docker compose up -d postgres
```

### Проблема: Порт 3000 уже занят

**Решение:**
1. Измените `PORT` в `.env` файле backend
2. Или остановите процесс, использующий порт 3000

### Проблема: Миграции не применяются

**Решение:**
```bash
# Проверить статус миграций
npm run db:migrate:status

# Применить миграции вручную
npm run db:migrate:prod

# Если нужно сбросить БД (ОСТОРОЖНО - удалит все данные!)
npm run db:migrate:reset
```

### Проблема: Frontend не подключается к Backend

**Решение:**
1. Проверьте, что Backend запущен на правильном порту
2. Проверьте `NEXT_PUBLIC_API_URL` в `.env.local`
3. Проверьте CORS настройки в Backend

### Проблема: Ошибки при установке зависимостей

**Решение:**
```bash
# Очистить кэш npm
npm cache clean --force

# Удалить node_modules и package-lock.json
rm -rf node_modules package-lock.json

# Переустановить
npm install
```

---

## 📝 Полезные команды

### Backend

```bash
# Запуск в режиме разработки
npm run start:dev

# Запуск в production режиме
npm run start:prod

# Применение миграций
npm run db:migrate:prod

# Генерация Prisma Client
npm run db:generate

# Открыть Prisma Studio (GUI для БД)
npm run db:studio

# Линтинг кода
npm run lint

# Форматирование кода
npm run format
```

### Frontend

```bash
# Запуск dev сервера
npm run dev

# Сборка для production
npm run build

# Запуск production сборки
npm start

# Линтинг
npm run lint
```

### Docker

```bash
# Запуск всех сервисов
docker compose up -d

# Остановка всех сервисов
docker compose down

# Просмотр логов
docker compose logs -f

# Пересоздание контейнеров
docker compose down -v
docker compose up -d
```

---

## 🎯 Следующие шаги

После успешного запуска:

1. **Протестируйте API:**
   - Откройте Swagger: http://localhost:3000/api/docs
   - Или используйте Postman: [POSTMAN_TESTING.md](POSTMAN_TESTING.md)

2. **Создайте первого пользователя:**
   - Зарегистрируйтесь через Frontend
   - Или используйте API endpoint `/api/auth/register`

3. **Изучите документацию:**
   - [DEVELOPMENT.md](DEVELOPMENT.md) - Руководство для разработчиков
   - [API.md](API.md) - Описание всех endpoints
   - [FEATURES.md](FEATURES.md) - Реализованный функционал

---

## 📚 Дополнительные ресурсы

- [DOCKER_SETUP.md](DOCKER_SETUP.md) - Подробное руководство по Docker
- [SERVER_REQUIREMENTS.md](SERVER_REQUIREMENTS.md) - Требования к серверу
- [DEVELOPMENT.md](DEVELOPMENT.md) - Руководство для разработчиков
- [POSTMAN_TESTING.md](POSTMAN_TESTING.md) - Тестирование API

---

**Время на полную установку:** 15-30 минут (в зависимости от скорости интернета)

**Готово!** Проект должен быть запущен и готов к разработке. 🚀
