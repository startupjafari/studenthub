# Конфигурационные файлы

Все необходимые конфигурационные файлы для NestJS проекта были созданы:

## ✅ Созданные файлы

### TypeScript
- **tsconfig.json** - Основная конфигурация TypeScript
- **tsconfig.build.json** - Конфигурация для сборки (исключает тесты)

### NestJS
- **nest-cli.json** - Конфигурация NestJS CLI

### Тестирование
- **jest.config.js** - Конфигурация Jest для unit тестов
- **test/jest-e2e.json** - Конфигурация для E2E тестов

### Code Quality
- **.eslintrc.js** - Конфигурация ESLint
- **.prettierrc** - Конфигурация Prettier

## 📋 Описание

### tsconfig.json
- Настроен для NestJS с поддержкой декораторов
- Target: ES2021
- Включена поддержка path aliases (@/*)
- Source maps включены

### nest-cli.json
- Source root: `src`
- Build output: `dist`
- Использует `tsconfig.build.json` для сборки

### jest.config.js
- Тесты в директории `src`
- Покрытие кода в `coverage`
- Поддержка TypeScript через ts-jest

## 🚀 Использование

Теперь можно запускать:

```bash
# Сборка
npm run build

# Запуск в dev режиме
npm run start:dev

# Тесты
npm run test

# E2E тесты
npm run test:e2e

# Линтинг
npm run lint

# Форматирование
npm run format
```

Все команды из package.json теперь должны работать корректно!

