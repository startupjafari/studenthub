# Инструкции по настройке проекта

## ⚠️ Важно: Первоначальная настройка

Перед запуском проекта необходимо выполнить следующие шаги:

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните все необходимые переменные в `.env` файле.

### 3. Генерация Prisma Client

**КРИТИЧЕСКИ ВАЖНО!** Перед запуском приложения нужно сгенерировать Prisma Client:

```bash
npx prisma generate
```

Или через npm скрипт:

```bash
npm run db:generate
```

Если скрипта нет, добавьте его в `package.json`:

```json
{
  "scripts": {
    "db:generate": "prisma generate"
  }
}
```

### 4. Выполнение миграций

После генерации Prisma Client, выполните миграции:

```bash
npm run db:migrate
```

Это создаст структуру базы данных в PostgreSQL.

### 5. Проверка подключения к Redis

Убедитесь, что Redis запущен:

```bash
redis-server
```

Или если используете Docker:

```bash
docker run -d -p 6379:6379 redis
```

### 6. Запуск приложения

Теперь можно запустить приложение:

```bash
npm run start:dev
```

## 🔧 Решение проблем

### Ошибка: "Module '@prisma/client' has no exported member 'UserRole'"

**Решение:** Выполните генерацию Prisma Client:

```bash
npx prisma generate
```

Эта команда создаст типы на основе вашей Prisma schema.

### Ошибка: TypeScript configuration file not found

**Решение:** Убедитесь, что файлы `tsconfig.json` и `nest-cli.json` существуют в корне проекта.

### Ошибка подключения к базе данных

**Решение:** 
1. Проверьте, что PostgreSQL запущен
2. Проверьте `DATABASE_URL` в `.env` файле
3. Убедитесь, что база данных создана

### Ошибка подключения к Redis

**Решение:**
1. Проверьте, что Redis запущен
2. Проверьте `REDIS_HOST` и `REDIS_PORT` в `.env` файле
3. Проверьте, что порт 6379 не занят другим процессом

## 📋 Порядок команд для первой настройки

```bash
# 1. Установить зависимости
npm install

# 2. Создать .env файл
cp .env.example .env
# Отредактировать .env файл

# 3. Сгенерировать Prisma Client (ОБЯЗАТЕЛЬНО!)
npx prisma generate

# 4. Выполнить миграции
npm run db:migrate

# 5. Запустить Redis (в отдельном терминале)
redis-server

# 6. Запустить приложение
npm run start:dev
```

## ✅ Проверка работоспособности

После запуска приложения:

1. Откройте браузер и перейдите на: http://localhost:3000/api/docs
2. Должна открыться Swagger документация
3. Попробуйте выполнить `GET /api/health` для проверки

## 🚀 Готово!

После выполнения всех шагов, ваш Auth Module будет полностью готов к использованию!

